<div class="d-flex align-items-center">
  <h1><%= @params.month %> 月 <%= @params.day %>日 行動記録</h1>
  <div class="ml-auto boards__linkBox">
    <%= link_to '一覧へ戻る', :back, class: 'btn btn-outline-dark' %>
  </div>
</div>
<% if flash[:notice] %>
  <div class="alert alert-primary"><%= flash[:notice] %></div>
<% end %>
<div>
  <p>所属：</p>
  <p>氏名：<%= @current_user.name %></p>
</div>

<%= render partial: 'form', locals: { actionlog: @actionlog } %>
